[project]
name = "django-totp"
dynamic = ["version"]

authors = [{ name = "Qcial Labs", email = "contact@qcial.com" }]

maintainers = [{ name = "Qcial Labs", email = "contact@qcial.com" }]

description = "Production-ready TOTP (Time-based One-Time Password) support for Django and Django REST Framework."
readme = "README.md"

license = { text = "MIT" }

requires-python = ">=3.12"

dependencies = [
    "cryptography>=47.0.0",
    "django>=5.0",
    "djangorestframework>=3.15",
    "djangorestframework-simplejwt>=5.5.1",
    "pyotp>=2.9.0",
    "qrcode>=8.2",
]

keywords = [
    "django",
    "totp",
    "2fa",
    "otp",
    "authentication",
    "security",
    "drf",
    "django-rest-framework-totp",
    "django-totp",
    "django-two-factor-auth",
    "python-totp",
]

classifiers = [
    "Development Status :: 5 - Production/Stable",
    "Framework :: Django",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Topic :: Security",
    "Topic :: Software Development :: Libraries",
]

[project.urls]
Homepage = "https://QcialDotCom.github.io/django-totp/"
Documentation = "https://QcialDotCom.github.io/django-totp/"
Repository = "https://github.com/QcialDotCom/django-totp"
Issues = "https://github.com/QcialDotCom/django-totp/issues/"
Company = "https://qcial.com"

[build-system]
requires = ["setuptools>=68", "wheel", "setuptools-scm>=8"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
package-dir = { "" = "src" }

[tool.setuptools.packages.find]
where = ["src"]
include = ["django_totp*"]
exclude = ["django_totp_project*"]

[tool.setuptools_scm]
local_scheme = "no-local-version"

[dependency-groups]
dev = [
    "djoser>=2.3.3",
    "drf-spectacular>=0.29.0",
    "pytest>=9.0.3",
    "pytest-django>=4.12.0",
]

[tool.pytest.ini_options]
DJANGO_SETTINGS_MODULE = "django_totp_project.settings"
python_files = ["test_*.py", " *_tests.py"]
python_classes = ["Test*", "Post*"]
